<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fatihdemirag.net.dersprogram">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity android:name=".DersEkle" />
        <activity android:name=".Custom_ListView" />
        <activity android:name=".Custom_AlertDialog" />
        <activity
            android:name=".Gunler"
            android:label="@string/title_activity_gunler"
            android:theme="@android:style/Theme.Material.Light" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Material.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DersNotlari"
            android:theme="@android:style/Theme.Material.Light" />
        <activity
            android:name=".DersNotuEkle"
            android:theme="@android:style/Theme.Material.Light" />
        <activity
            android:name=".DersListesiNot"
            android:label="Ders Notları"
            android:theme="@android:style/Theme.Material.Light" />
        <activity android:name=".Custom_ListView2" />
        <activity
            android:name=".DersNotuGor"
            android:theme="@android:style/Theme.Material.Light" />
        <activity android:name=".Ayarlar"></activity>
    </application>

</manifest>